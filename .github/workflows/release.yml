on:
  pull_request:
    types: [ opened ]

name: Generate release

jobs:
  release:
    name: Generate release
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.pull_request }}
